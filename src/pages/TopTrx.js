import React from 'react';
import { Layout } from './Layout';
import { TopTransaksiList } from '../components/TopTransaksiList';

export const TopTrx = () => {
  return (
    <Layout>
        <TopTransaksiList/>
    </Layout>
  )
}