import React from 'react';
import { Layout } from './Layout';
import { MemberList } from '../components/MemberList';

export const Member = () => {
  return (
    <Layout>
        <MemberList/>
    </Layout>
  )
}