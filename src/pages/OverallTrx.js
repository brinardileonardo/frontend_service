import React from 'react';
import { Layout } from './Layout';
import { OverallTransaksiList } from '../components/OverallTransaksiList';

export const OverallTrx = () => {
  return (
    <Layout>
        <OverallTransaksiList/>
    </Layout>
  )
}